[project]
name = "podcast-producer"
version = "0.1.0"
description = "Automatically generate and review Twitter clips from podcast episodes"
requires-python = ">=3.11"
dependencies = [
    "assemblyai>=0.33.0",
    "anthropic>=0.39.0",
    "ffmpeg-python>=0.2.0",
    "flask>=3.0.0",
]
